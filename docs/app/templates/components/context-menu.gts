import pageTitle from 'ember-page-title/helpers/page-title';

import ContextMenu from '../../components/f/components/context-menu';

<template>
  {{pageTitle "Context Menu"}}

  <div class="container mx-auto">
    <ContextMenu />
  </div>
</template>
