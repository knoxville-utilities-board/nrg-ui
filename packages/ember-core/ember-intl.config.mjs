export default {
  lintRules: {
    // Currently can't follow all paths that determine a key,
    // like in validations. This is likely a difficult, if
    // not impossible, problem to solve.
    'no-unused-keys': false,
  },
};
