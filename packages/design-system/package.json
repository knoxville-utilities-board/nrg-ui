{
  "name": "@nrg-ui/css",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:sass": "sass --load-path node_modules src:dist",
    "watch:sass": "sass --load-path node_modules --watch src:dist",
    "start": "concurrently pnpm:*:sass",
    "build": "concurrently pnpm:build:sass",
    "sasstest": "sass --help"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "bootstrap": "5.3.3",
    "bootstrap-icons": "^1.11.3",
    "concurrently": "^8.2.2",
    "sass": "^1.54.8"
  }
}
